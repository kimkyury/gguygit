---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BookReader from '../../components/BookReader.astro';

export function getStaticPaths() {
  return [];
}

const { slug } = Astro.params;
---

<BaseLayout title={`Bundles: ${slug}`}>
  <h1>Bundles Detail</h1>
  <BookReader summary="보따리 카테고리용 공통 책 형식 리더 템플릿입니다.">
    <p>Slug: {slug}</p>
    <p>좌우 화살표 키로 스프레드를 넘기고, 모바일에서는 스크롤로 읽습니다.</p>
  </BookReader>
</BaseLayout>
