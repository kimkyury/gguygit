---
import BaseLayout from '../../layouts/BaseLayout.astro';

const { slug } = Astro.params;
---

<BaseLayout title={`Culture: ${slug}`}>
  <h1>Culture Detail</h1>
  <p>Slug: {slug}</p>
</BaseLayout>
