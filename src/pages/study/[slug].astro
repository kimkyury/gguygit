---
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [];
}

const { slug } = Astro.params;
---

<BaseLayout title={`Study: ${slug}`}>
  <h1>Study Detail</h1>
  <p>Slug: {slug}</p>
</BaseLayout>
