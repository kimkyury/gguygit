---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BookReader from '../../components/BookReader.astro';
import PostHeroHeader from '../../components/PostHeroHeader.astro';

export function getStaticPaths() {
  return [];
}

const { slug } = Astro.params;
---

<BaseLayout title={`Meals: ${slug}`}>
  <article class="category-post">
    <PostHeroHeader
      category="끼니"
      title="Meals Detail"
      summary="끼니 카테고리용 공통 책 형식 리더 템플릿입니다."
      rightMeta={slug ?? ''}
    />
    <BookReader summary="끼니 카테고리용 공통 책 형식 리더 템플릿입니다.">
      <p>Slug: {slug}</p>
      <p>이 페이지는 와이드에서 2페이지 스프레드, 모바일에서 스크롤 모드로 동작합니다.</p>
    </BookReader>
  </article>
</BaseLayout>

<style>
  .category-post {
    display: grid;
    gap: 28px;
  }
</style>
