---
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [];
}

const { slug } = Astro.params;
---

<BaseLayout title={`Cook: ${slug}`}>
  <h1>Cook Detail</h1>
  <p>Slug: {slug}</p>
</BaseLayout>
