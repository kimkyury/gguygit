---
import BaseLayout from '../../layouts/BaseLayout.astro';

export function getStaticPaths() {
  return [];
}

const { slug } = Astro.params;
---

<BaseLayout title={`Goods: ${slug}`}>
  <h1>Goods Detail</h1>
  <p>Slug: {slug}</p>
</BaseLayout>
