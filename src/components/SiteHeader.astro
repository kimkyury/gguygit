<header class="site-header">
  <div class="site-brand">
    <a class="site-title" href="/">GGuGit|꾸깃</a>
  </div>
  <div class="site-meta">
    <span> </span>
  </div>
</header>

<style>
  .site-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 24px;
    padding: 34px 0 10px;
    width: min(var(--page-width), calc(100% - var(--page-pad-x) * 2));
    margin: 0 auto;
  }

  .site-brand {
    display: grid;
    gap: 8px;
  }

  .site-title {
    font-family: 'JN_leanring', var(--font-display);
    font-size: clamp(1.5rem, 2.6vw, 2.05rem);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
  }

  .site-tagline {
    margin: 0;
    color: var(--color-muted);
    font-size: 0.92rem;
  }

  .site-meta {
    display: grid;
    gap: 4px;
    font-size: 0.76rem;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--color-muted);
    text-align: right;
    opacity: 0.86;
  }

  @media (max-width: 760px) {
    .site-header {
      align-items: center;
    }

    .site-meta {
      display: none;
    }
  }
</style>
